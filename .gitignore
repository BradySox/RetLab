*.pyc
__pycache__
build/**
# Sphinx
docs/_build
resources/payloads/*.lua
venv
.venv/
# Embedded Python interpreter (shipped at runtime, never committed)
.python311/
.python311-embed.zip
run_retribution.bat
.DS_Store
.vscode/settings.json
dist/**
/.coverage
/coverage.xml
# User-specific stuff
.idea/
.env
env/

/kneeboards
/liberation_preferences.json
/state.json
/serverconfig.env

/logs/
/resources/logging.yaml
/tmp/

*.psd
_wpntools/
_scenerytools/

# Machine-local DCS backup, written by qt_ui/liberation_install.py
# replace_mission_scripting_file() from *your* DCS install on launch. It is not
# source: tracking it pushed one machine's DCS state to everyone, and the copy
# upstream shipped was a stale Retribution replacement (sanitizeModule calls
# commented out), so "Restore original" wrote a DESANITIZED file back to DCS.
# Untracked so each install captures its own faithful stock backup.
resources/scripts/MissionScripting.original.lua

# Aircraft module manuals copied out of the DCS install (E:\DCS World\Mods\aircraft\*\Doc).
# ~820 MB of vendor PDFs — local reference only, never source. The README and the per-module
# INDEX.md page maps are tracked so lookups keep working on a checkout without the PDFs;
# the PDFs themselves are not.
/references/manuals/*
!/references/manuals/README.md
!/references/manuals/*/
/references/manuals/*/*
!/references/manuals/*/INDEX.md

# mypy
.mypy_cache/
CLAUDE.local.md
