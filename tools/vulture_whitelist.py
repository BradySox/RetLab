# Names vulture reports unused that a signature requires. Read by vulture only;
# never imported or executed. See tools/dead_code_report.py.
# fmt: off
# flake8: noqa

memo  # __deepcopy__(self, memo)
exc_val  # __exit__(self, exc_type, exc_val, exc_tb)
exc_tb
line_number  # Qt message handler signature
close_event  # Qt closeEvent override
